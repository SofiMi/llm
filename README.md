# Telegram RAG Bot

Telegram-бот с функциональностью RAG для анализа и поиска информации по Telegram каналам.

## 🚀 Основные возможности

- **Парсинг Telegram каналов** - автоматический сбор сообщений из публичных каналов
- **Векторный поиск** - поиск релевантной информации с помощью ChromaDB и эмбеддингов
- **LLM интеграция** - генерация ответов на основе найденного контекста с помощью Mistral AI
- **Контекстные диалоги** - поддержка многооборотных диалогов с учетом истории
- **Управление пользователями** - система регистрации и сессий
- **Статистика** - мониторинг загруженных данных и производительности

## 🛠 Технический стек

- **Python 3.10+**
- **aiogram 3.3.0** - Telegram Bot API
- **ChromaDB** - векторная база данных для поиска
- **PostgreSQL 15** - хранение пользовательских данных
- **Mistral AI API** - генерация ответов
- **sentence-transformers** - создание эмбеддингов
- **Telethon** - парсинг Telegram каналов
- **Docker & Docker Compose** - контейнеризация

## 📋 Требования

- Python 3.10+
- PostgreSQL 15+
- Docker и Docker Compose (опционально)
- API токены:
  - Telegram Bot Token
  - Mistral API Key (опционально)

## ⚡ Быстрый старт

### 1. Клонирование и установка

```bash
git clone <your-repo>
cd llm
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Обязательные параметры
BOT_TOKEN=your_telegram_bot_token_here
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Опциональные параметры
MISTRAL_API_KEY=your_mistral_api_key_here

# Настройки базы данных (если не используете DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=llm_bot
DB_USER=your_db_user
DB_PASSWORD=your_db_password
```

### 3. Запуск через Docker Compose (рекомендуется)

```bash
docker-compose up -d

docker-compose logs -f bot
```

### 4. Локальный запуск

```bash
python src/main.py
```

## 🎯 Использование

### Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начало работы с ботом и регистрация |
| `/session` | Начать новую LLM сессию |
| `/stop` | Остановить текущую сессию |
| `/add_channel <ссылка>` | Добавить Telegram канал для анализа |
| `/stats` | Показать статистику RAG базы данных |

### Примеры использования

1. **Добавление канала:**
   ```
   /add_channel https://t.me/your_channel
   ```

2. **Начало сессии и вопрос:**
   ```
   /session
   Расскажи о последних новостях в криптовалютах
   ```

3. **Контекстный диалог:**
   ```
   Пользователь: Что думают о Bitcoin?
   Бот: [ответ на основе данных каналов]
   Пользователь: А что насчет Ethereum?
   Бот: [ответ с учетом контекста предыдущего вопроса]
   ```

## 📁 Структура проекта

```
llm/
├── src/                          # Исходный код
│   ├── main.py                   # Точка входа
│   ├── rag_integration.py        # RAG система
│   ├── bot/                      # Telegram бот
│   │   ├── bot.py               # Основная логика бота
│   │   ├── config.py            # Конфигурация
│   │   ├── db.py                # Работа с БД
│   │   ├── session_context.py   # Управление контекстом
│   │   └── handlers/            # Обработчики команд
│   │       ├── llm_session.py   # LLM сессии
│   │       └── registration.py  # Регистрация
│   └── sql/                      # SQL схемы
│       └── init.sql
├── raw_datasets/                 # Сырые данные
├── requirements.txt              # Python зависимости
├── docker-compose.yml           # Docker конфигурация
├── Dockerfile                   # Docker образ
├── .env                         # Переменные окружения (создать)
└── README.md                    # Этот файл
```

## 🔧 Разработка

### Установка зависимостей для разработки

```bash
pip install -r requirements.txt
```


### Отладка

1. **Логи бота:**
   ```bash
   docker-compose logs -f bot
   ```

2. **Проверка базы данных:**
   ```bash
   docker exec -it my_postgres psql -U myuser -d mydb
   ```

3. **Мониторинг ChromaDB:**
   - ChromaDB данные сохраняются в папке `./chroma_db`
   - Используйте команду `/stats` для проверки состояния

## ⚙️ Конфигурация

### Основные параметры

- **RAG_SYSTEM_PROMPT** - системный промпт для LLM
- **Batch размеры** - настройка в `rag_integration.py` для оптимизации памяти
- **Модель эмбеддингов** - `paraphrase-multilingual-MiniLM-L12-v2` (поддержка русского языка)

### Оптимизация производительности

1. **Память:**
   - Система автоматически мониторит использование памяти
   - Адаптивные паузы между операциями
   - Batch обработка документов

2. **База данных:**
   - Пул соединений PostgreSQL
   - Оптимизированные запросы ChromaDB

## 🐛 Известные ограничения

- Максимальный размер документа для ChromaDB: 50,000 символов
- Ограничение на количество сообщений за раз: 100
- Требуется доступ к публичным Telegram каналам
- Mistral AI необходим для полной функциональности LLM

## 📝 Лицензия

Этот проект распространяется под лицензией MIT.

## 🚧 Планы развития

- [ ] Поддержка дополнительных LLM провайдеров (OpenAI, Anthropic)
- [ ] Веб-интерфейс для управления
- [ ] Поддержка приватных каналов
- [ ] Экспорт и импорт данных
- [ ] Метрики и аналитика использования
- [ ] Поддержка файловых вложений

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте логи: `docker-compose logs -f bot`
2. Убедитесь в правильности переменных окружения
3. Проверьте доступность внешних API (Telegram, Mistral)
4. Создайте issue в репозитории с описанием проблемы

---

**Примечание:** Для полной функциональности убедитесь, что все зависимости установлены и API ключи настроены корректно.
